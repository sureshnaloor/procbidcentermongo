import type { Metadata } from "next";
import "./globals.css";
import { Providers } from "@/components/providers";
import { Toaster } from "sonner";

export const metadata: Metadata = {
  title: {
    template: "%s | ProcBidCenter",
    default: "ProcBidCenter — Procurement & Bidding Platform",
  },
  description:
    "Streamlined procurement and bidding platform for EPC companies and suppliers. Manage tenders, bids, vendor qualifications, and more.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="antialiased">
        <Providers>
          {children}
          <Toaster richColors position="top-right" />
        </Providers>
      </body>
    </html>
  );
}
